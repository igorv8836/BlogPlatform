package support

enum class ModAction { HIDE, DELETE, RESTORE }

enum class AuthorRole { USER, MODERATOR, SYSTEM }

enum class MessageType { MESSAGE, CLARIFICATION_REQUEST, ANSWER, MODERATION_NOTE }

enum class TicketStatus { OPEN, IN_PROGRESS, WAITING_USER, ANSWERED, CLOSED }